from django.http import JsonResponse


def login(request):
    # TODO
    return JsonResponse({
        'isSuccess': True
    })


def register(request):
    # TODO
    return JsonResponse({
        'isSuccess': True
    })
